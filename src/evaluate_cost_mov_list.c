#include <R.h>
#include <Rinternals.h>
#include <stdio.h>

SEXP evaluate_cost_mov_list(SEXP input, SEXP mov_list, SEXP changed_list, SEXP solution, SEXP alpha) {

  SEXP result_mov_list = PROTECT(allocVector(REALSXP, 1));

  
  
  UNPROTECT(1);
  return(result_mov_list);
}

